Ramin Yazdani | pymol-hydrophobicity-tools | main | feat(extension): Optimize smooth_values with edge case handling

Enhanced the smooth_values function with better edge case handling and 
performance optimizations.

Improvements:
- Better handling when window extends beyond list end
- More efficient window calculation
- Validation for edge cases (empty lists, window_size=1, etc.)
- Code optimization for large protein sequences

Edge cases handled:
- window_size = 1: Return original values (no smoothing needed)
- Empty input list: Return empty list
- Window extends beyond end: Handle gracefully with available values

Rationale:
The extension will be used with various proteins and parameter combinations. 
Robust edge case handling ensures the extension works reliably across all 
usage scenarios without crashes or unexpected behavior.

This matches the CLI tool's smoothing implementation, ensuring consistent 
results between batch analysis and interactive visualization.

Verified: Function handles all edge cases correctly, optimized performance tested.
